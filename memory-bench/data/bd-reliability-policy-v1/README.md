# bd memory policy candidate v1

Selected after 24 development sessions on one exposed EnterpriseBench episode. This is a candidate for prescribed memory use; reliability and task benefit remain unestablished.

`capture-review.md` is the exact development-03 evidence-card instruction chain for reviewing supplied historical evidence. `read.md` is the exact development-01 JSON discovery contract. Attach the corresponding task request separately. Explicitly request lookup when testing prescribed use; the read contract itself does not require optional lookup.

The capture candidate produced one supported, useful persisted record, with partial quotation/locator precision. Later read applicability additions did not establish task gains, so the simpler tested discovery procedure is retained. This selection is a development decision, not a causal policy ranking. Actual coding followed by capture requires a separately frozen execution trigger; historical-review instructions alone do not define that workflow.
